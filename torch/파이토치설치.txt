conda create -n dl_study python=3.10 -y
conda deactivate
conda activate dl_study

장치관리자에서 그래픽카드 알아보고 최신버전 업데이트 후 작업
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install ipykernel jupyter
python -m ipykernel install --user --name dl_study --display-name "DL_RTX4060Ti"

주피터노트북 실행
jupyter notebook 

주피터노트북 내에서 DL_RTX4060Ti 선택해서 파일 만들고 작업
import torch
print(torch.cuda.is_available())
- True 가 나와야 함
print(torch.cuda.get_device_name(0))
- NVIDIA GeForce RTX 4060 Ti 자신의 컴퓨터 그래픽카드 

아나콘다 네비게이터에 주피터노트북이 안보이고 Environments에도 안뜨는데 Environments에서라도 뜨게 할려면 아래와 같이 설치
conda install ipykernel notebook